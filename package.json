{
  "name": "docchina-webapp",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check app.js && node --check firebase-config.js && node --check game-logic.js && node --check game-config.js && npm test",
    "test": "node --test tests/*.test.mjs",
    "serve": ".venv/bin/python -m http.server 4173",
    "vendor:budoux": "mkdir -p vendor && cp node_modules/budoux/bundle/budoux-ja.min.js node_modules/budoux/bundle/budoux-ja.min.js.map vendor/"
  },
  "dependencies": {
    "budoux": "0.7.0"
  }
}
