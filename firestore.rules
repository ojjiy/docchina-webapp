rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 短命なゲームルームのみを扱うMVP向けルールです。Firebase App Checkを併用してください。
    match /rooms/{roomId} {
      allow read, create, update: if roomId.matches('^[A-Z2-9]{6}$');
      allow delete: if false;
    }
  }
}
